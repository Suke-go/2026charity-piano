# Copy to .env.local for local use.
# Leave empty for nginx-backed local deployment and the Vite dev proxy.
# Set a full origin only when intentionally bypassing the local reverse proxy.

VITE_API_BASE_URL=
VITE_DEV_ACCESS_TOKEN=
VITE_DEFAULT_EVENT_ID=local-feedback
